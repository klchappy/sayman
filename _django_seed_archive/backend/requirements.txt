# Sayman — Multi-Tenant Muhasebe SaaS — Production runtime requirements
# Faz 15 Production Hardening Patch ile dondurulmuştur (seed).
# Faz A Multi-Tenant Foundation patch ile django-tenants eklendi.
#
# Kuralları:
# - Yalnız production runtime için gerekli paketler.
# - Tüm sürümler dondurulmuş (==) — repro install için zorunlu.
# - Selenium / playwright / docx2pdf gibi UI/scrape araçları YOK (production runtime'a gerek yok).
# - Telegram bot kütüphanesi YOK (Faz 13 dry-run; gerçek gönderim Faz 16+ kararı).
#
# Yükleme:
#   pip install -r requirements.txt

# --- Django core ---
Django==6.0.4
asgiref==3.11.1
sqlparse==0.5.5
tzdata==2026.1                # Windows + bazı Linux dağıtımları için TZ veri tabanı

# --- Excel export (reports / Faz 12) ---
openpyxl==3.1.5
et_xmlfile==2.0.0

# --- Multi-tenant (Sayman Faz A) ---
# django-tenants 3.10.1 (Mart 2026), Django 6.0 + Python 3.13 destekli
# Schema-per-tenant pattern; PUBLIC schema = control plane, her tenant kendi schema'sı
django-tenants>=3.10,<4

# --- PostgreSQL driver + production server ---
# Not: django-tenants postgresql_backend psycopg3 ile uyumlu
psycopg[binary]>=3.1,<4
gunicorn>=21.2,<24

# --- Faz 15 risk notu ---
# Django 6.0.4 sürümü mevcut ortamda dondurulmuştur.
# Faz 14 W6 uyarısı: 6.x LTS değildir. İleride 5.2 LTS'e geri çekme veya 6.x bakımıyla devam
# kararı _docs/PHASE15_PRODUCTION_HARDENING_REPORT.md "Django Version Decision" bölümünde
# kayıtlıdır.
